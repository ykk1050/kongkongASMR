.DS_Store
Thumbs.db
node_modules/
*.log
__pycache__/

# 확인용 스크린샷은 저장소 뿌리에 떨어진다 (Playwright 를 프로젝트 폴더에서 돌린다).
# 한 번 `git add -A` 로 1.9MB 를 딸려 올린 적이 있어 아예 막아 둔다.
# 게임이 쓰는 그림은 assets/ 안에 있으므로 이 규칙에 걸리지 않는다.
/*.png
