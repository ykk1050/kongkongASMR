# 작업 규칙

## 배포 — 작업은 `main` 까지 올린다

이 저장소는 **`main` 브랜치를 그대로 서빙**합니다 (워크플로 없이 `.nojekyll` + GitHub Pages).
즉 `main` 에 없는 것은 https://ykk1050.github.io/kongkongASMR 에 **존재하지 않습니다.**

작업 브랜치에만 푸시하고 "푸시했습니다"라고 보고한 적이 있습니다. 브랜치에는 분명히
올라갔지만 게임에는 아무것도 바뀌지 않아, 같은 지적을 세 번 받았습니다.

그래서:

- 기능이 끝나면 **`main` 에 병합해 푸시**한다. 브랜치에만 두지 않는다.
- 세션 지시 때문에 작업 브랜치를 써야 한다면, **`main` 에 올릴지 먼저 묻는다.**
  "브랜치에 푸시했다"는 보고만 하고 넘어가지 않는다.
- 사용자가 "반영이 안 됐다"고 하면 코드보다 **`git log origin/main` 을 먼저** 본다.
  배포 브랜치에 그 커밋이 있는지가 첫 번째 확인 사항이다.
- 푸시 뒤에는 캐시 때문에 안 보일 수 있으므로 강력 새로고침을 안내한다.

## 내보낼 때마다 버전을 올린다

화면 맨 아래에 `Created by gamtudyssam using AI · 0.00027` 이 적혀 있습니다
(`index.html` 의 `#loadNote`). **내보내는 변경마다 0.00001 씩 올립니다.**

```bash
python tools/stamp.py --bump    # 버전 +0.00001 · 캐시 도장도 함께 찍는다
```

손으로 고치면 잊어버리고, 부동소수로 더하면 `0.00028000000000000004` 가 됩니다.
그래서 도구가 정수로 세서 올립니다.

## 확인 방법

브라우저 없이 고쳤다고 말하지 않습니다. 로컬 서버를 띄우고 Playwright 로 직접 확인합니다.

```bash
python -m http.server 5178
```

```js
SK.Game.debug.state()            // 페이즈 · 기대 토큰 · 글자 타일
SK.Game.debug.tileState(i, j)    // 한 칸의 damage/broken/solid — 부활 여부 확인용
SK.Game.debug.stepOn(i, j, 1)    // 해당 칸에 착지
SK.Game.debug.solidConnected()   // 성한 발판이 하나로 이어져 있는가
await SK.Audio.loudnessTable()   // 재질별 체감 음량(K-가중) — 음량 밸런스는 이걸로
```

## 게임 규칙 중 깨뜨리면 안 되는 것

- **부서진 타일은 되살아나지 않는다.** 복구 타이머를 다시 넣지 않는다.
- 그래서 **답을 못 밟는 판이 생기지 않게** 세 가지가 걸려 있다 — 구멍을 서로 맞닿게
  뚫지 않기, 사라지면 판이 끊기는 타일은 부서지지 않기, 글자가 얹힌 타일이 부서지면
  글자를 성한 칸으로 옮기기. 하나라도 빼면 갇히는 판이 나온다.
- 2회 틀렸을 때 **정답 타일을 직접 알려 주지 않는다.** 글 힌트만 켠다.
- **한 문제에 재질은 두 가지.** 글자 타일 한 종류 + 바닥 한 종류다. 칸마다 다른 재질을
  뿌리면 한 발짝마다 소리가 바뀌어 ASMR이 효과음 모음집이 된다. 바닥은 문제가 바뀔 때
  갈아 끼운다. `SK.Game.debug.matMix().kinds` 가 2인지로 확인한다.
